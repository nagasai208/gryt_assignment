import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const MainDiv = styled.div`
${tw`flex justify-between m-5`}`;

const DropDown = styled.select`
${tw`h-5 w-32`}`


export { MainDiv, DropDown}