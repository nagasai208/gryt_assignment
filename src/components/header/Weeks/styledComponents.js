import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const MainDiv = styled.div`
${tw`  bg-gray-600 m-5 `}`

const WeeksDiv = styled.div`
${tw`flex m-5 justify-around w-full`}
`



export { MainDiv, WeeksDiv,  }